node('workstation') {
    try {
        stage("one") {
            echo "one"
            exit 1
        }

        stage('two') {
            echo "two"
        }
    } catch (e) {
        stage('Declarative: Post Actions') {
            echo 'This will run only if failed'
        }

    }

}

